{% set form = hookable_metadata.context.form.gatewayConfig.config.loggerLevel %}
<div class="card mb-3">
    <div class="card-body">
        <div class="row">
            {{ form_row(form) }}
        </div>
    </div>
</div>

{% set order = hookable_metadata.context.resource.orders|last %}
<tbody>
    {% for payment in order.payments %}
        <tr>
            {% hook 'body' with { payment } %}
        </tr>
    {% endfor %}
</tbody>
